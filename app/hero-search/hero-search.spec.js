'use strict';

describe('myApp.heroSearch module', function() {

  beforeEach(module('myApp.heroSearch'));

  describe('heroSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var HeroSearchCtrl = $controller('HeroSearchCtrl');
      expect(HeroSearchCtrl).toBeDefined();
    }));

  });
});